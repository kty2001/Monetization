# TODO

크롤러 → 파이프라인 → 모델 → 배포 앱까지 전 단계 구현 완료. 이 문서는 **남은 작업**을 우선으로 정리하고, 완료 항목은 요약만 남김. 설계 근거는 [로드맵](04_로드맵.md), 성능 상세는 [모델 명세](06_모델.md) 참고.

## 남은 작업

### 모델 개선

- [ ] **`N`(피처 기준 회차 수) 선택 미결** — N=20이 8.2% 개선(logRMSE 0.9036 → 0.8294, MdAPE 51.3% → 47.4%)이지만 추론 대상이 11,189 → 5,737로 절반이 됨. N=10 유지 / N=20 전환 / 2단 구성 중 결정 필요. 위 수치는 총 구매수 타겟 기준이라 **회차당 타겟으로 재측정한 뒤** 판단할 것
- [ ] 신규 피처 탐색 — 회차당 좋아요·댓글 비율, 조회수 감소 기울기, 연재 주기(`published_at` 간격) 등. 하이퍼파라미터는 여력이 없고 피처가 병목임이 확인됨
- [ ] `tags` 상위 K개 토큰 multi-hot 재검토 (현재는 결측률 시프트로 제외)

### 데이터

- [ ] `scripts/monthly_snapshot.ps1` 생성 + 작업 스케줄러 등록 — 스크립트 내용과 `schtasks` 명령은 [크롤러](03_크롤러.md) "정기 스냅샷 운영"에 기재 완료, 실행만 남음
- [ ] `pl.serial`(진행 중 1,212건) 크롤 — 학습 제외·정성 검증용이라 후순위. 다만 전향적 학습셋을 노린다면 **전환 감지 경로**로 필요함(갓 전환된 작품은 `pl.serial_end`가 아니라 여기로 감)
- [ ] novels ↔ novel_stats join 정책 결정 — 2시점 기준 64건이 stats에 없어 결측이 됨. inner join으로 버릴지 left join으로 둘지 미결

### 앱 배포

- [ ] 코드 서명 인증서 — 없으면 첫 실행 시 SmartScreen "알 수 없는 게시자" 경고가 계속 뜸(현재는 `사용법.txt` 안내로 대응)
- [ ] 스냅샷 갱신 시 재빌드·재배포 절차 정립 — 자동 업데이트 없음
- [ ] 실행 속도가 문제되면 onedir + zip으로 되돌리는 것을 재검토 (전달 편의 ↔ 매 실행 7~8초의 교환)

## 완료 요약

### 1. 크롤러 (`clawler/`)

- [x] 무료 자유연재 전체 크롤 — **48,897작품 / 431,782회차** (run_id `20260810_231806`)
- [x] 유료 전환작 라벨 크롤 — **9,252작품 / 234,058회차** (`pl.serial_end`, run_id `20260812_174443`)
- [x] 목록 통계 백필 + **2시점 스냅샷** 확보, 이후 **월 1회 / 앞 200페이지** 운영으로 확정
- [x] `run_crawl` 확장으로 유료 섹션 대응 — 별도 `paid_runner.py` 없이 `--section`/`--prefix`/`--collect-stats` 추가
- [x] `--free-chapters-only` — 첫 유료 회차에서 중단해 크롤 9시간대 → 2시간대
- [x] `--start-page` — `--resume` 시 앞 페이지 재스캔에 20분 이상 버리던 문제 해결
- [x] `--max-pages 0`/`--novel-limit 0` = 제한 없음 — 기본값 1을 빠뜨리면 조용히 20건만 수집하고 정상 종료하던 문제 대응
- [x] 워치독 `scripts/watch_and_resume.ps1` — 프로세스 감시 + 정체 감지 + `--start-page` 자동 계산 + 무한 재시작 방지
- [x] `N=10` 가정 전수 재확인 — 연속성 위반 0건, 무료 회차 중앙값 25, 타겟 비-0 99.7%
- [x] 지지구간 재측정 — 범위 외삽은 44.4% → **0.6%로 해소**, 그러나 무료작 84%가 학습셋 5분위수 미만이라 **밀도 불일치는 잔존**

### 2. 데이터 파이프라인 (`service/`, `repository/`)

- [x] `data/raw/` → `data/processed/` 정제 (`raw_loader`/`novel_features`/`target_builder`/`schema` + `scripts/build_processed_dataset.py`)
- [x] 앞 10화 기준 피처 — 추론셋 **11,189건** / 학습셋 **8,990건**
- [x] `snapshot_date`(YYYYMMDD) 기반 누적 저장
- [x] **범주형 피처 결함 수정** — `serialization_status`(분할과 교락)와 `tags`(결측률 시프트)를 제거, `genres`는 토큰 단위 multi-hot으로 전환. 재발 방지 테스트 추가
- [x] `leading_free_episodes` 버그 수정 — 앞 N화로 자른 뒤 세어 항상 10이 나오던 것을 자르기 전에 세도록 수정(메타데이터이므로 모델에는 영향 없음)
- [x] `Novel` 스키마 중간 변경으로 CSV가 11/13필드로 깨졌던 것을 복구 + `append_csv` 헤더 가드 추가

> 제목의 U+FEFF는 피처에 제목을 쓰지 않아 정제 단계에서 다루지 않고, 화면 표시 직전(`scripts/build_app_bundle.py`)에 제거함.

### 3. 모델 (`research/`, `scripts/`)

- [x] `HistGradientBoostingRegressor` 채택 — logRMSE **0.6947** / logR² **0.869** / MdAPE **40.6%** / Spearman **0.919**, 단일회귀 베이스라인 대비 **19.7% 개선**
- [x] **DL 도입 불필요** 판정 — 개선폭이 CV 표준편차의 10배가 넘어 노이즈로 설명되지 않음
- [x] **타겟 정의 오류 수정** — `nvSumPurchased`가 작품 전체 누적인데 회차당으로 표시해 매출이 100배 넘게 부풀려졌음(회차당 1,110만원 → 수정 후 61,600원). 유료 회차 수로 나눈 뒤 성능도 함께 개선(logRMSE 0.9036 → 0.6947)
- [x] 테스트 데이터 성능 파악 — holdout 1,798건 기준 ±25% 이내 34.9% / 2배 이내 70.4% / 5배 이내 96.9%
- [x] 하이퍼파라미터 튜닝 여력 조사 — 25조합 랜덤서치로 CV logRMSE 0.5% 개선에 그쳐 **적용 보류**
- [x] 선택 편향 대응 — `support_band` 컬럼(정상 16.0% / 희박 84.0%)을 예측 출력에 포함

> 매출(KRW)은 모델이 예측하지 않음. `예상 매출 = 예측 구매수 × 회차 단가`이고, 단가는 작가별 계약에 따라 달라 크롤 불가하므로 앱에서 입력받아 곱함.

### 4. 배포용 데스크톱 앱 (`app/`)

- [x] ~~Streamlit 대시보드~~ **폐기** — 수요자가 비전공자 작가인데 `streamlit run`이 Python 설치를 전제함. `pages/`·`streamlit` 의존성 제거
- [x] **PyInstaller + Tkinter 앱 구현** — 카탈로그 조회(11,189건) + 작품 주소 실시간 크롤·예측
- [x] **onefile 단일 exe** `dist/MunpiaRevenue.exe` **72.1MB** — 파일 하나만 전달하면 실행됨(onedir은 `_internal/` 151MB가 있어야 해 exe만 넘기면 실패했음). 대가인 매 실행 7~8초는 스플래시(0.4초 표시)로 대응
- [x] 회차 단가 입력 위젯 → `예측 구매수 × 단가` 실시간 환산
- [x] `support_band` 경고 · "전환 성공 시 조건부 기댓값" 문구 UI 노출 + 작가용 `사용법.txt` 동봉
- [x] 빌드 자동화 `scripts/build_desktop_app.ps1` (번들 → 스플래시 → 빌드 → 자체 점검)
- [x] 실사용 피드백 반영 — 창 1920×1080 기준 + 폰트 확대, 검색 범위 문구를 수집 시점 기준으로 정정

### 5. 테스트 (`tests/`)

- [x] **129개 통과** (`uv run pytest`) — 크롤러·엔티티·리포지토리·데이터 품질·피처/타겟/스키마·모델 파이프라인·앱 추론 로직. 항목별 분포는 [로드맵 E](04_로드맵.md) 참고

## 참고 문서

- [환경설정](01_환경설정.md) · [프로젝트 개요](02_프로젝트개요.md) · [크롤러](03_크롤러.md)
- [로드맵](04_로드맵.md) · [데이터 사전](05_데이터사전.md) · [모델 명세](06_모델.md) · [실행 가이드](07_실행가이드.md)
